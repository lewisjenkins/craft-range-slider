{% import "_includes/forms" as forms %}

{% do view.registerAssetBundle("lewisjenkins\\craftrangeslider\\assetbundles\\rangesliderfield\\RangeSliderFieldAsset") %}

{% set config = {
    id: 'options',
    name: 'options',
    value: field.options,
    class: 'nicetext code',
    rows: field.initialRows
} %}


{{ forms.textarea(config) }}